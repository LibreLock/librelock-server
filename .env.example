APP_ENV=development
PORT=8000

# Postgres
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=librelock
DB_PASSWORD=secret
DB_NAME=librelock
# Or provide a full DSN:
# DATABASE_URL=postgres://librelock:secret@127.0.0.1:5432/librelock?sslmode=disable

TOKEN_TTL=3600

# Frontend origin allowed for CORS
ALLOWED_ORIGIN=http://localhost:1401
