services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DB_PATH: /app/data/librelock.db
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data:
